image: maven:3.8-openjdk-11

default:
  tags:
    - gitlab-org-docker

stages:
  - test

Simple test with allure:
  stage: test
  script:
    - mvn clean test
    - echo "Test started!!!"
  after_script:
    - mvn allure:report
  artifacts:
    when: always
    paths:
      - target/site/allure-maven-plugin

